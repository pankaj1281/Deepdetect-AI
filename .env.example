# Backend
MONGO_URI=mongodb://localhost:27017
MONGO_DB=deepdetect

# Model weights (leave unset to use random-initialised models)
IMAGE_MODEL_PATH=
VIDEO_MODEL_PATH=
AUDIO_MODEL_PATH=

# CORS — comma-separated list of allowed origins (use * for development)
ALLOWED_ORIGINS=http://localhost:3000
